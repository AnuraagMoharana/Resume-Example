# Static assets

This directory is reserved for static site assets.

Recommended organization:

```text
assets/
├── css/
├── js/
├── images/
└── fonts/
```

The current portfolio keeps its live `styles.css` and `script.js` at the repository root to preserve the existing GitHub Pages paths. Move them here only together with the corresponding `index.html` path updates and a browser test.
